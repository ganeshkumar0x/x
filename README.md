# Linux Setup Scripts

Two simple scripts to set up a minimal Linux environment with a window manager.

## Scripts

- **`setup_system.sh`** - run this to install system packages, dependencies, and system configuration (`./setup_system.sh`)
- **`setup_wm.sh`** - run this to install the WM and symlink the dotfiles (`./setup_wm.sh`)
